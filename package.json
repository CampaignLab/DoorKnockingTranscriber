{
  "name": "door-knocking-notes",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Privacy-first, fully on-device door knocking notes: record and transcribe (Whisper). No cloud.",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "idb": "^8.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^1.2.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-pwa": "^0.21.1",
    "vitest": "^2.1.4"
  }
}
